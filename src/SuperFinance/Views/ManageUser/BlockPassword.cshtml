@model BlockPasswordModel

@{
	ViewBag.Title = "Block Password";
}

<section class="form" id="blockPasswordForm">
	@using (Html.BeginForm())
	{
		<h1> @ViewBag.Title </h1>
		<hr />

		@Html.AntiForgeryToken()

		<fieldset>
			<div class="form-group">
				@Html.LabelFor(m => m.Reason, new { @class = "label-big" })
				@Html.TextBoxFor(m => m.Reason, new { @class = "form-control", placeholder = "Please enter reason" })
				@Html.ValidationMessageFor(m => m.Reason)
			</div>

			<br />
			<div class="form-group">
				<button type="submit" class="btn btn-primary">Block Password</button>
			</div>

		</fieldset>
	}
</section>